<UserControl x:Class="Presentation.View.Settings_View.SettingsUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             FlowDirection="RightToLeft"
             Background="#EEF2F7">

    <!--<UserControl.Resources>
        <Style x:Key="SidebarTabStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="Transparent" CornerRadius="10" Padding="15,12" Margin="0,2">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1F5F9"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EFF6FF"/>
                                <Setter Property="Foreground" Value="#3498db"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/> <ColumnDefinition Width="*"/>   </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="0,15,15,0" Margin="0,0,10,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="#000" Opacity="0.05" ShadowDepth="2"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="20,25">
                    <TextBlock Text="قائمة الإعدادات" FontSize="18" FontWeight="Bold" Foreground="#1E293B"/>
                    <TextBlock Text="تحكم في خصائص النظام" FontSize="12" Foreground="#64748B" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="10,0">
                    <RadioButton x:Name="BtnDeviceSettings" Content="💻 تصنيفات الأجهزة" IsChecked="True" 
                                 Style="{StaticResource SidebarTabStyle}" Click="DeviceSettings_Click"/>

                    <RadioButton x:Name="BtnCompanySettings" Content="🏢 بيانات الشركة" 
                                 Style="{StaticResource SidebarTabStyle}" Click="CompanySettings_Click"/>

                    <RadioButton x:Name="BtnPrinterSettings" Content="🖨️ إعدادات الطباعة" 
                                 Style="{StaticResource SidebarTabStyle}" Click="PrinterSettings_Click"/>

                    <RadioButton x:Name="BtnBackupSettings" Content="💾 النسخ الاحتياطي" 
                                 Style="{StaticResource SidebarTabStyle}" Click="BackupSettings_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" x:Name="SettingsSubContent" Margin="20">
            </Grid>
    </Grid>-->
</UserControl>